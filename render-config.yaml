services:
  - type: web
    name: webfrontend
    env: static
    buildCommand: npx vite build
    publishDir: dist
    staticPublishPath: /
    headers:
      - path: /*
        headers:
          Cache-Control: no-cache
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
